#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running type checks before push..."
npm run -s typecheck || {
  echo "❌ Type checks failed. Use 'git push --no-verify' to bypass (not recommended)."
  exit 1
}
echo "✅ Type checks passed."
